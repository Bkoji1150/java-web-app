pipeline {
  agent { label 'docker' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Scan') {
      steps {
       sh 'terraform -version'
       sh 'sudo mvn --version /home/agent/jenkinsagent/workspace/'
      }
    }
  }
}
