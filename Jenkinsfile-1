pipeline {
   tools {
        maven 'mvn'
        jdk   'jdk'
    }
  agent { label 'docker' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Scan') {
      steps {
       sh 'terraform -version'
       sh 'mvn --version'
      }
    }
  }
}
